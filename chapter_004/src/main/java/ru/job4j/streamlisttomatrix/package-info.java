package ru.job4j.streamlisttomatrix;

/**
 *  Конверитировать
 *  List<Integer>  в  int[]
 *
 *  Надо делать что-то из этой серии:
 *  String[] number = collection.stream()
 *  .flatMap((p) -> Arrays.asList(p.split(",")).stream())
 *  .toArray(String[]::new);
 */
